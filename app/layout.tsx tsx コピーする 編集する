export const metadata = { title: "p2pChat" };

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ja">
      <body style={{ fontFamily: "system-ui, -apple-system, Segoe UI, Roboto, sans-serif", background: "#0b0c10", color: "#e5e7eb", margin: 0 }}>
        {children}
      </body>
    </html>
  );
}
